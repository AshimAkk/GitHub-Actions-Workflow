name: My GitHub Workflow 
on: [push]

# Push event triggers the entire workflow below 

jobs:
  testing_github:
    runs-on: ubuntu-latest
    steps:
      - name: Hello message
        run: echo "Hello, GitHub Actions"
      - name: Display repo name
        run: echo "This Workflow is running in ${{github.repository}}"